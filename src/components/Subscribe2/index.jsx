import React, { memo } from 'react'
import styles from './style.module.scss'

function Subscribe2() {
   return <section className={styles.Subscribe2}>Subscribe2</section>
}

export default memo(Subscribe2)
