import React, { memo } from 'react'
import styles from './style.module.scss'

function BlogCategoryPage() {
   return <div className={styles.BlogCategoryPage}>BlogCategoryPage</div>
}

export default memo(BlogCategoryPage)
