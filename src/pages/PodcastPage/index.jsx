import React, { memo } from 'react'
import styles from './style.module.scss'

function PodcastPage() {
   return <div className={styles.PodcastPage}>PodcastPage</div>
}

export default memo(PodcastPage)
