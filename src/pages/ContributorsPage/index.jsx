import React, { memo } from 'react'
import styles from './style.module.scss'

function ContributorsPage() {
   return <div className={styles.ContributorsPage}>ContributorsPage</div>
}

export default memo(ContributorsPage)
