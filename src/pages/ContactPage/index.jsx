import React, { memo } from 'react'
import styles from './style.module.scss'

function ContactPage() {
   return <div className={styles.ContactPage}>ContactPage</div>
}

export default memo(ContactPage)
